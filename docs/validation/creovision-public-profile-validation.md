# Validación de perfil público

En `http://127.0.0.1:8080/perfil/test-user`, con el backend de perfiles inaccesible, la ruta mostró `Perfil temporalmente no disponible` y explicó que no se muestran datos privados ni se asume que el perfil sea público. El título quedó `Perfil público no disponible` y el estado se marcó noindex por la condición de backend no disponible.
